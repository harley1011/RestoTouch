export function main() {
  describe('registering into the website', () => {

    beforeEach(() => {
    });

    it('registered after completing form correctly', function() => {
      expect(2).toEqual(1);//testing to see if kama shows me it fails
    });

    it('unregistered after wrong input', function() => {

    });

  });

}
